# ui

- Compose 画面および共通コンポーネントを機能別に配置するホーム階層。
- `home` `drawer` `settings` `ai` が画面単位、`components` が再利用 UI、`theme` が Material 設定を担当する。
- `discord` が Discord WebView/通知/設定セクションをまとめ、ViewModel と画面を同じフォルダに置く。
